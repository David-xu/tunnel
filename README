#1.参数列表
--mode=[client/server]
--serv_ip=1.1.1.1
--port_list=49000,49001,49002

server模式:

client模式:
--local_agent_port_list=1080,1090
该参数指定本地代理的端口列表,只有client模式需要.client会监听该参数指定的列表,当被connect时会向server发送控制报文触发server创建代理实例,并与server端本地的对应端口建联

#2.隧道协议
a)数据散列到不同传输层管道上发送
b)支持加密
c)支持限速

#3.传输层
a)协商密钥




# client模式
./fgfw --mode=client --serv_ip=127.0.0.1 --port_list=40000,40001 --local_agent_port_list=30000
# server模式
./fgfw --mode=server --serv_ip=127.0.0.1 --port_list=40000,40001

# 测试
./fgfw --testbench=30000 --mode=server
./fgfw --testbench=30000 --mode=client